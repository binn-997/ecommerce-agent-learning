# ADR-002：硬规则不用 LLM 自证

- 状态：Accepted
- 日期：2026-07-16

## 决策

标题长度、五点数量、绝对化宣传、医疗声明、主关键词和 GPSR 元数据完整性由 Pydantic 与确定性代码检查。法务适用性、证据充分性和最终发布由人判断。

## 原因与后果

硬规则必须能做零漏检回归和明确定位；模型输出不能同时充当被检对象与裁判。新增规则需要版本、样例和回归测试。规则命中只生成 block/warn，不自动修改或发布 Listing。
