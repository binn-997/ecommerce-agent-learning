# ADR-001：使用显式决策图而不是单个大 Prompt

- 状态：Accepted
- 日期：2026-07-16

## 决策

Listing 和广告诊断使用显式节点：读授权证据、结构化生成/计算、确定性检查、人工接管。每个节点携带 `request_id`/`trace_id`，并限制重试次数；checkpoint 以业务请求键幂等恢复。

## 原因与后果

运营事实、模型生成、硬规则和人工法律判断的失败语义不同。拆分后可以单独测试、重放和阻断；代价是状态契约与 checkpoint 需要维护。图停止在草稿/建议，不包含发布、改价、广告写入或采购工具。
